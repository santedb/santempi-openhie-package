apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: santedb
  name: santedb-service
spec:
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8083
      targetPort: 8080
    - name: "2100"
      port: 2100
      targetPort: 2100
  selector:
    io.kompose.service: santedb
